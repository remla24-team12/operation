apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "remla-istio.fullname" . }}-test-connection"
  labels:
    {{- include "remla-istio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "remla-istio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

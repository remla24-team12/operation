apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "remla.fullname" . }}-test-connection"
  labels:
    {{- include "remla.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "remla.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
